[package]
name = "rig-client"
version.workspace = true
edition.workspace = true

[dependencies]
tokio = { version = "1.0", features = ["full"] }
# TODO: Add RIG framework and Claude API dependencies when implementing
